# Upload the jet here

Upload the GLB into this folder with this exact filename:

`flanker_vr_quest3.glb`

The flight test already loads:

`./assets/models/flanker_vr_quest3.glb`

Once the GLB is uploaded, reload the page. The HUD will report how many expected VR setup nodes it found, including `PilotEye`, `SeatAnchor`, `FlightStickGrip`, `FlightStickPivot`, `ThrottleGrip`, and `ThrottlePivot`.
